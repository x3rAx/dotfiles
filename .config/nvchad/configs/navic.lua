local config = {
    lsp = {
        auto_attach = true,
    },
    click = true, -- Enable single / double click on elements
    highlight = true, -- Use higlight groups to colorize icons and text
    separator = "  ",
}

return config
