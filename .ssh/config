# Add the following line to a host to make it use ssh-agent:
#
# ```
# Host ...
#     AddKeysToAgent no | yes | confirm [time-interval] | ask | time-interval
# ```

Host sudocode
    HostName localhost
    IdentityFile ~/.ssh/sudocode_ed25519
    User root

Host jarvis badwolf
    AddKeysToAgent ask

